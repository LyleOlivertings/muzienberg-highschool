/** @type {import('next').NextConfig} */
const nextConfig = {  images: {
    domains: [
      'via.placeholder.com', // For placeholder images
      'your-custom-domain.com' // Add your actual domain later
    ],
  },};

export default nextConfig;
